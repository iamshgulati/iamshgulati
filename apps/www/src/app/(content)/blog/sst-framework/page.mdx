export const metadata = {
  title: "Experience With SST",
  description:
    "Recent experience with SST Framework during development of a serverless application on AWS.",
};

todo

export default function ContentPage({ children }) {
  return <ContentLayout metadata={metadata}>{children}</ContentLayout>;
}
